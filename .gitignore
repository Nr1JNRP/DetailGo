# OSX
.DS_Store

# Xcode
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata
*.xccheckout
*.moved-aside
DerivedData
*.hmap
*.ipa
*.xcuserstate
**/.xcode.env.local
**/.xcode.env

# Android/IntelliJ
.idea
.gradle
local.properties
*.iml
*.hprof
.cxx/
.kotlin/
# Chaves sensíveis Android (use secrets.properties.example como template)
android/secrets.properties

# ── React Native build artifacts (gerados em tempo de build, nunca commitar) ──
android/app/src/main/assets/index.android.bundle
android/app/src/main/assets/index.android.bundle.map
android/app/src/main/res/raw/keep.xml

# ── SEGURANÇA: Keystores (nunca subir chaves de assinatura) ──────────────────
*.keystore
*.jks
# debug.keystore também ignorado — não expor chave de assinatura

# ── SEGURANÇA: Firebase / Google Services ────────────────────────────────────
# Contém API keys, project IDs e configurações do Firebase
google-services.json
GoogleService-Info.plist

# ── SEGURANÇA: Variáveis de ambiente e secrets ────────────────────────────────
.env
.env.local
.env.production
.env.staging
.env.development
*.secret
*.secrets

# ── SEGURANÇA: Chaves privadas e certificados ────────────────────────────────
*.pem
*.p12
*.key
*.cer
*.der
serviceAccountKey.json
*service-account*.json
*credentials*.json

# ── SEGURANÇA: Firebase Functions secrets locais ────────────────────────────
functions/.env
functions/.env.*
functions/.secret.*

# node.js
node_modules/
npm-debug.log
yarn-error.log

# Bundle artifact
*.jsbundle

# fastlane
**/fastlane/report.xml
**/fastlane/Preview.html
**/fastlane/screenshots
**/fastlane/test_output

# Ruby / CocoaPods
**/Pods/
/vendor/bundle/

# Metro
.metro-health-check*
metro.log
metro.err.log

# Testing
/coverage

# Firebase emulator logs (gerados por firebase emulators:exec)
firebase-debug.log
firestore-debug.log
ui-debug.log
pglite-debug.log
firebase-export-*

# Generated APKs
/dist/apk/

# Yarn
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Claude Code (pastas temporárias de desenvolvimento)
.claude/
